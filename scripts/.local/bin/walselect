#!/bin/bash

PROVIDED_PATH=""
NOTIFY=""
ECHO=""

while [ $# -gt 0 ]; do
	case "$1" in
	--path | -path | --p | -p)
		PROVIDED_PATH="$2"
		shift 2
		;;
	--notify | notify | --n | -n)
		NOTIFY="true"
		shift
		;;
	--echo | echo | --e | -e)
		ECHO="true"
		shift
		;;
	*)
		shift
		;;
	esac
done

